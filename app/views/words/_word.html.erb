<div class="content">
  <div class="content__right">
    <div class="content__right__top">
      <%= link_to word.title, word_path(word.id), class: "content__right__top--title" %>
      <%# <%= link_to "削除", word_path(word.id), method: :delete, class: "wordManage__delete" %> 
    </div>
    <div class="content__right__bottom">
      <div class="content__right__bottom--userName">
        <%= link_to word.user.name, user_path(word.user), class: "content__right__bottom--userName--btn" %>
        <%# <%= word.user.name %>
      </div>
        <%# <div class="content__right__bottom--date">
        <%= word.created_at %>
        <%# </div> %> 
      </div>
  </div>
</div>
